[step]
name = "009-two-pass-symbols"
status = "pending"
prompt = """
Implement two-pass assembly with symbol table.

1. Implement first pass: scan all lines, collect label addresses into
   sym_names[] / sym_addrs[] parallel arrays.
2. Track current address during first pass (account for variable-length
   instructions and directives).
3. Implement symbol lookup: find_symbol(name) returns address or -1.
4. On second pass, resolve label references in branch/jump/la operands.
5. Test with programs containing both backward and forward label references.
6. Verify output matches cross-assembler.

Reference: Rust assembler's two-pass logic and symbol table.
"""
