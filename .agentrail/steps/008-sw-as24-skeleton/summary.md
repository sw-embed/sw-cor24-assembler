Step 008 exit criteria MET: just build produces build/sw-as24.bin (106 bytes); just test PASSES byte-identical (ref=0xFF from cor24-run --assemble nop.s; candidate=0xFF from sw-as24's hex-encoded UART match-path output 'SFF' after banner-strip + hex2bin decode). Significant scope expansion required: (1) cloned cor24-rs, built newer cor24-run binary with --load-binary support; (2) vendored it (updated version.json: repo=cor24-rs, commit=40033d90, binary_src=rust-to-cor24/target/release/cor24-run); (3) extended vendor-fetch.sh with binary_src manifest field + $SW_EM24_BIN override + system-PATH fallback; (4) fixed lc r0, 255 -> lcu r0, 255; (5) fixed build.sh to propagate cor24-run --assemble errors (printed to stdout, exit 0); (6) discovered cor24-run filters 0x00 from every UART TX observation path; (7) adopted hex-encoded UART output per user direction (sw-as24 emits 'FF' or '00' ASCII hex pairs); (8) wrote scripts/hex2bin.sh decoder; (9) rewrote scripts/test.sh pipeline (--load-binary + awk parse UART output: + strip banner + hex2bin); (10) corrected nop encoding from 0x00 (wrong initial assumption) to 0xFF (verified); (11) stripped comment from nop.s so saga-1 sw-as24 can parse it; (12) updated docs/design.md. Commits on feat/relaunch-project: 4147254 (sw-as24 draft), e7364a3 (lcu + build.sh asm error detect), 1f50b62 (vendor repoint + fetch extensions), 12c13e8 (hex pipeline), 7f92076 (design doc update).