Wrote scripts/build.sh (62 lines) + chmod +x. Invokes 'cor24-run --assemble <input.s> <output.bin> <output.lst>' matching sw-cor24-ocaml's proven usage. Loads vendor/active.env to resolve $SW_EM24_VERSION and the vendored binary path. Creates build/ if missing. Error paths (missing active.env -> exit 4; missing vendored cor24-run -> exit 1 with vendor-fetch hint; missing src/sw-as24.s -> exit 1 with step-008 hint) are all exercised and clean. 'just build' on a fresh clone propagates the missing-binary failure through the recipe. Success path deferred until a vendored cor24-run is available. Committed 6393017.