Relaunch saga closed. Cold-state validation green: rm -f vendor/.../bin/cor24-run + rm -rf build/ -> just vendor-fetch resolves cor24-rs sibling at commit 40033d90, copies cor24-run -> just build assembles 106 bytes -> just test passes byte-identical (ref=0xFF from cor24-run --assemble nop.s; candidate=0xFF from sw-as24 hex-ASCII 'SFF' -> banner strip -> hex2bin). docs/plan.md restructured: Relaunch moved to Completed sagas with as-shipped exit criteria + 11-step outcome table; saga 2 (Lexer and line parsing) promoted to Current with anticipated deliverables including comment stripping (closes saga-1 workaround on nop.s), real byte-to-hex helper, and nibble<->ASCII primitives shared with future sw-hexload. Committed 2ce79ae. Agentrail history shows all 11 steps completed. feat/relaunch-project ready for rename/PR targeting dev -- flagged as next HUMAN action, not performed in this step per step prompt.