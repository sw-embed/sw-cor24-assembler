Wrote justfile with six recipes (vendor-fetch, vendor-check, build, test, run FILE, clean) plus a 'default' that shows the recipe list. set dotenv-load := true auto-sources vendor/active.env so  reaches the run recipe without a hardcoded version. build does not auto-invoke vendor-fetch (fail-fast via scripts/build.sh missing-binary check). Committed aa8e0ac. VERIFICATION GAP: 'just' is not installed in this build environment so 'just --list' / 'just clean' could not be run. Grammar follows sw-cor24-ocaml precedent verbatim. Gap to be closed by a future step that has just on PATH.