Wrote scripts/vendor-fetch.sh (147 lines). Ported from sw-cor24-ocaml's version: kept bootstrap + helpers (SCRIPT_DIR/REPO_ROOT, --check mode, active.env source, manifest_get via jq, resolve_local_repo); dropped fetch_pascal/fetch_pcode (we vendor only sw-em24); dropped the system-installed cor24-run fallback for reproducibility reasons. All error paths tested without a populated sibling: missing active.env -> exit 4; unknown arg -> exit 2; missing manifest -> exit 1; missing sibling -> exit 1 with clone+cargo hint; --check with missing binary -> exit 1 with MISSING. Committed df42b42. Success path (copying a real cor24-run) deferred until an upstream sibling exists in this worktree.