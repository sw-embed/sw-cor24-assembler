Wrote tests/smoke/nop.s (5 lines, single nop mnemonic) and scripts/test.sh (140 lines, three-stage harness). Pipeline: reference bytes via cor24-run --assemble; candidate bytes via cor24-run --run with UART feed; diff -q with od hexdump on failure. Error paths (missing env/binary/input) all clean. Committed 5c4f1d8. VERIFICATION GAPS: (1) UART-output capture format on stdout is unverified -- script probes --help for --uart-out and falls back to parsing 'UART output:' lines with explicit TBD comments at the edit points; (2) success path requires both a vendored cor24-run and sw-as24.bin (the latter comes from step 008). Neither is blocking because the error paths prove the harness wiring end-to-end.